# This is an example file. It will
# be consumed by docker-compose when
# starting services. Do not edit it directly.
# Copy it to `.env` instead. Do not commit `.env`

PG_VERSION=14

# Once connected to the same net, the host name is the container name
PG_HOST=database

PG_PORT=5432
PG_DATABASE=postgres
PG_USERNAME=postgres
PG_PWD=notarealpassword

APP_PORT=8501

ADMINER_VERSION=4.8.1
ADMINER_PORT=8080
